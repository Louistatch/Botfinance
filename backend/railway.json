{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"npx prisma migrate deploy || echo '[BOOT] AVERTISSEMENT: prisma migrate deploy a echoue — verifiez la variable DATABASE_URL et le service PostgreSQL'; node dist/main.js\"",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
